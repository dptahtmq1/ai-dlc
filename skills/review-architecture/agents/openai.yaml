interface:
  display_name: "Review Architecture"
  short_description: "Review architecture and route AI rework"
  default_prompt: "Use $review-architecture to independently review the current architecture proposal inside its AI loop."
