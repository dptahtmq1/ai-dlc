interface:
  display_name: "Draft Requirements"
  short_description: "Draft requirements for independent AI review"
  default_prompt: "Use $draft-requirements to create the next version in an authorized requirements AI review loop."
