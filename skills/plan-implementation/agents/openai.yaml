interface:
  display_name: "Plan Implementation"
  short_description: "Plan implementation for independent AI review"
  default_prompt: "Use $plan-implementation to create the next implementation plan version inside an authorized AI review loop."
