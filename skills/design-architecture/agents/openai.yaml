interface:
  display_name: "Design Architecture"
  short_description: "Draft architecture for independent AI review"
  default_prompt: "Use $design-architecture to create the next architecture version inside an authorized AI review loop."
