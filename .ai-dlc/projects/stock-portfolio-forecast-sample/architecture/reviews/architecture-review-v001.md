# 아키텍처 검토 보고서

## 1. 메타데이터

| 필드 | 값 |
|---|---|
| 프로젝트 ID | `stock-portfolio-forecast-sample` |
| 검토 산출물 ID | `ARCH-REVIEW-001` |
| 검토 버전 | `v001` |
| 검토 대상 | `.ai-dlc/projects/stock-portfolio-forecast-sample/architecture/drafts/architecture-v001.md` (`v001`, `PROPOSED`) |
| 요구사항 기준선 | `.ai-dlc/projects/stock-portfolio-forecast-sample/requirements/baseline.md` (`v006`, `HUMAN_APPROVED`, 승인 `APR-20260715-007`) |
| 검토 역할 | `architecture-reviewer` |
| 검토 호출 | `INV-ARCH-REVIEWER-20260715-001` |
| 루프 승인 | `APR-20260715-008` |
| 루프 반복 | `1/3` |
| 상태 | `RECOMMENDATION_ONLY` |
| 작성 시각 | `2026-07-15T10:51:12.6114570+09:00` |

작성 호출 `INV-ARCH-AUTHOR-20260715-001`과 검토 호출 `INV-ARCH-REVIEWER-20260715-001`은 서로 다르며, 활성 상태의 작성 역할과 검토 역할도 분리되어 독립성 전제는 충족한다.

## 2. 권고

`REWORK_REQUIRED`

This AI review is a recommendation only; the orchestrator controls loop routing and human approval.

## 3. 요약

제안서는 승인된 개발 단계 범위를 유지하면서 기능 요구사항 5개, 비기능 요구사항 5개 및 품질 시나리오 5개를 설계 결정, 컴포넌트, 인터페이스와 검증 방법에 모두 연결했다. 특정 공급자나 구현 기술을 임의 선택하지 않았고 `OPEN-001`~`OPEN-009`를 `OPEN-ARCH-001`~`OPEN-ARCH-009`로 보존한 점도 적절하다.

그러나 사용자 웹 호출을 신뢰 경계에서 제외한 서술은 외부 입력과 보유정보가 시스템으로 유입되는 실제 경계를 누락한다. 또한 실행 증거 저장 계약은 원자성 및 완료 판정을 요구하면서 중단·부분 기록의 탐지와 재시도 의미를 정의하지 않아 재현성 품질 시나리오의 실패 경로가 닫히지 않는다. 두 결함은 현재 루프 안에서 수정 가능하다.

## 4. 범위

| 측정 항목 | 결과 |
|---|---:|
| Must 요구사항 추적 범위 | `100% (10/10)` |
| 품질 시나리오 추적 범위 | `100% (5/5)` |
| 검토 차원 완료 | `10/10` |

추적 범위는 ID 연결 여부를 뜻하며, 아래 finding으로 지적한 경계 및 실패 계약의 충분성을 합격으로 판정한다는 뜻은 아니다.

## 5. Findings

### ARCH-FIND-001: 사용자 웹 경계가 신뢰 경계에서 누락됨

- 심각도: `High`
- 신뢰도: `HIGH`
- 차단 여부: `Yes`
- 영향 ID: `DRV-001`, `DRV-006`, `FR-022`, `FR-026`, `CMP-001`, `CMP-002`, `INT-001`, `INT-005`, `DATA-001`
- 근거: 제안서 3절은 외부 데이터 출처와 시스템 사이를 "유일한 외부 데이터 신뢰 경계"라고 규정하고 사용자 경계를 애플리케이션 경계에서 제외한다. 하지만 `INT-001`은 웹 UX에서 보유 종목·수량과 버전 참조를 받아 시스템 내부 조정 모듈로 전달하며, `INT-005`는 실행 결과와 보유 기반 분석 정보를 웹 UX에 반환한다. `INT-001`의 필드 검증만으로 호출 주체, 입력 신뢰 수준, 결과 노출 경계를 설명할 수 없다.
- 위반 규칙: 아키텍처 검토 정책 3, 4, 7, 10 및 아키텍처 계약의 신뢰 경계·인터페이스 보안 의미 명시 규칙.
- 영향: 구현자가 브라우저/사용자 입력을 내부 신뢰 데이터처럼 취급하거나, 개발 환경 접근 제한과 애플리케이션 입력 검증의 책임을 혼동할 수 있다. 이는 상품용 인증을 다시 도입하라는 뜻이 아니라 개발 단계에서도 필요한 입력·출력 신뢰 경계가 불명확하다는 문제다.
- 권고: 시스템 컨텍스트에 사용자/웹 UX와 내부 분석 조정 사이의 신뢰 경계를 표시한다. `INT-001`에는 신뢰되지 않은 필드 입력의 검증·크기 제한·허용 형식·오류 반환 책임을, `INT-005`에는 개발 환경 내부에서만 결과를 노출한다는 경계를 명시한다. 상품 운영용 인증·권한은 계속 제외하고 실행 환경 접근 제한은 외부 전제로 유지한다.
- 필수 결정 소유자: `architecture-author`; 실제 개인 데이터 사용 여부가 바뀌는 경우 `ACTOR-002`

### ARCH-FIND-002: 실행 증거 저장의 부분 실패와 복구 계약이 불완전함

- 심각도: `High`
- 신뢰도: `HIGH`
- 차단 여부: `Yes`
- 영향 ID: `DEC-003`, `NFR-014`, `QA-014`, `CMP-002`, `CMP-006`, `INT-006`, `DATA-005`, `RISK-003`, `ADR-003`
- 근거: `INT-006`은 실행 기록을 원자적으로 저장하고 필수 증거 저장 실패 시 완료로 표시하지 않는다고 명시하지만, 복구 방식을 구현 계획으로 미룬다. 장시간 실행과 웹 요청 수명을 분리한다고 한 8절까지 고려하면, 조정 모듈 또는 저장소 중단 시 부분 기록을 어떻게 식별하는지, 같은 실행 ID의 재시도가 덮어쓰기인지 거절인지, 완료 표식과 결과 기록의 순서가 무엇인지가 없다. `DATA-005`도 저장 실패 뒤 복구 상태를 정의하지 않는다.
- 위반 규칙: 아키텍처 검토 정책 4, 5, 6, 8 및 아키텍처 계약의 인터페이스 실패·복구 의미와 품질 전술 타당성 규칙.
- 영향: 중단된 실행이 완료로 오인되거나 동일 실행 ID의 충돌 기록이 생기면, 저장된 메타데이터로 같은 분석을 재실행한다는 `NFR-014`와 `QA-014`를 신뢰성 있게 검증할 수 없다.
- 권고: 특정 저장 기술을 선택하지 않고도 최소 상태 전이(`요청됨`, `실행 중`, `실패`, `완료`)와 완료 표식의 조건, 부분 기록 탐지, 동일 실행 ID 쓰기의 멱등성 또는 거절 규칙, 실패 후 재개·새 실행 생성 중 하나의 복구 원칙을 `INT-006`과 `DATA-005`에 정의한다. 저장 기술, 백업, 장기 보존은 계속 구현 계획 또는 범위 밖으로 둘 수 있다.
- 필수 결정 소유자: `architecture-author`; 저장 자산 보존 기간은 `ACTOR-003`

## 6. Findings 요약

| 심각도 | 미해결 | 비차단 |
|---|---:|---:|
| Critical | 0 | 0 |
| High | 2 | 0 |
| Medium | 0 | 0 |
| Low | 0 | 0 |

차단 finding: `ARCH-FIND-001`, `ARCH-FIND-002`.

## 7. 추적성 결함

활성 Must 요구사항과 품질 시나리오의 ID 누락은 없다. `FR/NFR/QA -> DRV/DEC -> CMP/INT -> validation` 연결은 100% 존재한다. 다만 `NFR-014`와 `QA-014`의 정상 흐름 연결만으로는 `ARCH-FIND-002`의 저장 실패·복구 경로를 충족하지 못한다.

역방향 점검에서도 승인된 범위를 벗어난 기능 결정은 발견되지 않았다. `DRV-007`과 보안·운영 제외 사항은 개발 단계 범위를 지키며, 주문·체결 인터페이스를 추가하지 않았다.

## 8. 사람의 미결정 사항

`OPEN-ARCH-001`~`OPEN-ARCH-009`는 요구사항 기준선의 `OPEN-001`~`OPEN-009`와 각각 연결되어 있으며 임의로 해결되지 않았다.

| ID | 사람 결정 | 소유자 | 제안서의 차단 시점 평가 |
|---|---|---|---|
| `OPEN-ARCH-001` | 대상 시장과 종목 유니버스 | `ACTOR-002` | 정상 종단 간 분석 전 필요 |
| `OPEN-ARCH-002` | 예측 대상과 기간 | `ACTOR-002` | 모델 설계 및 `INT-003` 구현 전 필요 |
| `OPEN-ARCH-003` | 데이터 출처와 접근 방식 | `ACTOR-002`, `ACTOR-003` | 어댑터 구현 및 시점 검증 전 필요 |
| `OPEN-ARCH-004` | 대형주 판정 규칙 | `ACTOR-002` | 적격성 실행 전 필요 |
| `OPEN-ARCH-005` | 포트폴리오 제약 | `ACTOR-002` | 목표 포트폴리오 실행 전 필요 |
| `OPEN-ARCH-006` | 신규 적격 대형주 추가 허용 | `ACTOR-002` | 후보 유니버스 구현 전 필요 |
| `OPEN-ARCH-007` | 반올림 방식과 허용오차 | `ACTOR-002`, `ACTOR-004` | 계산 품질 합격 판정 전 필요 |
| `OPEN-ARCH-008` | 데이터 최신성 기준 | `ACTOR-002` | 상태 품질 합격 판정 전 필요 |
| `OPEN-ARCH-009` | 지원 화면 크기 | `ACTOR-002` | 반응형 품질 합격 판정 전 필요 |

모든 미결정 사항에 소유자와 처분 시점이 있어 현재 finding으로 추가하지 않는다. 다만 구현 계획은 각 차단점 이전에 사람의 결정을 받아야 하며 이를 해결된 것으로 간주해서는 안 된다.

## 9. 이전 finding 해결

최초 아키텍처 검토이므로 이전 독립 아키텍처 finding은 없다. 요구사항 단계의 `HFN-20260715-003`에 따른 개발 단계 축약과 상품화 통제 제거는 제안서 범위에 유지되었다.

## 10. 권고 다음 조치

오케스트레이터는 남은 반복 용량 안에서 `ARCH-FIND-001`과 `ARCH-FIND-002`를 아키텍처 작성자에게 자동 재작업으로 전달해야 한다. 개정안은 상품 운영용 인증·권한이나 특정 저장 기술을 새로 도입하지 않고, 개발 단계에 필요한 신뢰 경계와 실행 증거 실패·복구 계약만 보완한 뒤 새로운 독립 검토를 받아야 한다.

This AI review is a recommendation only; the orchestrator controls loop routing and human approval.
